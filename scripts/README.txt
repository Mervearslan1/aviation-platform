Python one-shot generators for this Java/Maven repo (rename, Flyway SQL, aircraft JSON).
Maven does not compile them. IntelliJ may mark .py red until a Python SDK is attached;
that is expected and does not block Java. Keep these files in git so IDEA sees them as project files.
